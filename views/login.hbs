<div class="signup-wrapper d-flex justify-content-center align-items-center">
    <div class="signup-container">
        <h2>Login</h2>
        {{#if error}}
            <p class="error-message">{{error}}</p>
        {{/if}}
        <form action="/login" method="POST">
            <div class="form-group">
                <input type="email" name="email" placeholder="Email Address" required>
            </div>
            <div class="form-group password-container">
                <input type="password" id="password" name="password" placeholder="Password" required>
                <span id="togglePassword" class="toggle-icon">👁️</span>
            </div>
            <button type="submit" class="btn-submit">Login</button>
        </form>
        <p class="login-link">Don't have an account? <a href="/signup">Sign up</a></p>
    </div>
</div>

<style>
    /* Centering & Layout */
    .signup-wrapper {
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
    
    }

    .signup-container {
        background: white;
        padding: 40px;
        border-radius: 15px;
        box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        width: 100%;
        max-width: 400px;
        text-align: center;
        animation: fadeIn 0.5s ease-in-out;
    }

    h2 {
        font-size: 28px;
        margin-bottom: 20px;
        color: #333;
        font-weight: bold;
    }

    .form-group {
        position: relative;
        margin-bottom: 20px;
    }

    /* Input Fields */
    input {
        width: 100%;
        padding: 12px;
        border-radius: 8px;
        border: 1px solid #ccc;
        font-size: 16px;
        outline: none;
        transition: border 0.3s ease-in-out;
    }

    input:focus {
        border-color: #ff758c;
        box-shadow: 0 0 8px rgba(255, 117, 140, 0.5);
    }

    /* Password Field */
    .password-container {
        position: relative;
    }

    .toggle-icon {
        position: absolute;
        right: 15px;
        top: 50%;
        transform: translateY(-50%);
        cursor: pointer;
        font-size: 20px;
        color: #888;
        transition: color 0.3s ease-in-out;
    }

    .toggle-icon:hover {
        color: #ff758c;
    }

    /* Button */
    .btn-submit {
        background: linear-gradient(45deg, #ff758c, #ff7eb3);
        color: white;
        padding: 12px;
        border: none;
        border-radius: 8px;
        font-size: 18px;
        cursor: pointer;
        width: 100%;
        transition: background 0.3s ease-in-out;
    }

    .btn-submit:hover {
        background: #ff7eb3;
    }

    /* Links */
    .login-link {
        margin-top: 15px;
        font-size: 14px;
        color: #333;
    }

    .login-link a {
        color: #ff758c;
        text-decoration: none;
    }

    .login-link a:hover {
        text-decoration: underline;
    }

    .error-message {
        color: red;
        font-size: 14px;
        margin-bottom: 15px;
    }

    /* Animations */
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(-20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* Responsive Design */
    @media (max-width: 576px) {
        .signup-container {
            padding: 30px;
            border-radius: 12px;
        }
    }

    @media (max-width: 400px) {
        .signup-container {
            padding: 20px;
        }
    }
</style>

<script>
    // Password Toggle
    document.getElementById("togglePassword").addEventListener("click", function () {
        const passwordInput = document.getElementById("password");
        passwordInput.type = passwordInput.type === "password" ? "text" : "password";
        this.textContent = passwordInput.type === "password" ? "👁️" : "🙈";
    });
</script>


{{!-- First Line:

js
Copy code
passwordInput.type = passwordInput.type === "password" ? "text" : "password";
It checks the current type of the passwordInput field.
If the type is "password", it changes it to "text", making the password visible.
If the type is "text", it changes it back to "password", hiding the password.
This is a ternary operator, which is a shorthand for writing an if-else condition:

js
Copy code
if (passwordInput.type === "password") {
    passwordInput.type = "text"; // Show password
} else {
    passwordInput.type = "password"; // Hide password
}
2️⃣ Second Line:

js
Copy code
this.textContent = passwordInput.type === "password" ? "👁️" : "🙈";
this refers to the element that was clicked (togglePassword span).
this.textContent updates the text inside the span (eye icon).
If the password is hidden ("password" type), it shows 👁️ (eye).
If the password is visible ("text" type), it shows 🙈 (monkey covering eyes).
This is also a ternary operator, which is equivalent to:

js
Copy code
if (passwordInput.type === "password") {
    this.textContent = "👁️"; // Show eye icon
} else {
    this.textContent = "🙈"; // Show monkey icon
}
💡 How It Works
1️⃣ The user clicks the eye icon (👁️).
2️⃣ The script checks the password input type:

If it's "password", it changes to "text" (revealing the password).
If it's "text", it changes back to "password" (hiding the password).
3️⃣ The icon updates:
If the password is hidden, show 👁️.
If the password is visible, show 🙈. --}}
