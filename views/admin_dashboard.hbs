<!-- views/admin_dashboard.hbs -->
<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Admin Dashboard</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <a href="/create_user" class="btn btn-success ms-auto">+ Create New User</a>
    </div>
  </div>
</nav>

<div class="container mt-4">
  <div class="table-container">
    <h3 class="text-center mb-3">Manage Users</h3>
    <div class="table-responsive">
      <table class="table table-hover text-center">
        <thead>
          <tr>
            <th>ID</th>
            <th>Username</th>
            <th>Email</th>
            <th>Role</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          {{#each users}}
          <tr>
            <td>{{this.user_id}}</td>
            <td>{{this.name}}</td>
            <td>{{this.email}}</td>
            <td>{{this.role}}</td>
            <td>
              <form action="/delete_user/{{this.user_id}}" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger btn-sm">
                  <i class="bi bi-person-x"></i> Delete
                </button>
              </form>
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
    </div>
  </div>
</div>

<style>
  body {
    background: #f4f7f6;
    font-family: 'Poppins', sans-serif;
  }

  .navbar {
    background: linear-gradient(45deg, #021B79, #0A1931, #185ADB) !important;
    border-bottom: 2px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0px 3px 10px rgba(0, 0, 0, 0.2);
  }

  .navbar-brand {
    font-size: 1.5rem;
    font-weight: bold;
    color: white !important;
  }

  .navbar-toggler-icon {
    filter: invert(1); /* White icon */
  }

  .table-container {
    max-width: 1200px; /* Cap width on desktop */
    width: 100%; /* Fluid width */
    margin: 0 auto;
    padding: 15px;
    background: #ffffff;
    border-radius: 12px;
    box-shadow: 0px 0px 15px rgba(0, 0, 0, 0.1);
  }

  .table thead {
    background: linear-gradient(45deg, #FFD700, #32CD32);
    color: white;
    font-weight: bold;
  }

  .table tbody tr:hover {
    background-color: #C0FFC0 !important;
    transition: background 0.3s ease-in-out;
  }

  .btn:hover {
    opacity: 0.9;
    transform: scale(1.05);
    transition: all 0.3s ease-in-out;
  }

  .btn-success, .btn-danger {
    font-size: 1rem;
    padding: 8px 12px;
  }

  /* Responsive Adjustments */
  @media (max-width: 767px) {
    .container {
      padding: 10px; /* Smaller padding */
      margin-top: 2rem; /* Reduce top margin */
    }

    .navbar-brand {
      font-size: 1.2rem; /* Smaller title */
    }

    .table-container {
      padding: 10px; /* Smaller padding */
    }

    .table {
      font-size: 0.9rem; /* Smaller table text */
    }

    h3 {
      font-size: 1.5rem; /* Smaller heading */
    }

    .btn-success, .btn-danger {
      font-size: 0.9rem; /* Smaller buttons */
      padding: 6px 10px;
    }
  }
</style>